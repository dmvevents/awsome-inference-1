# OKRs — Dynamo inference on AWS EFA (rev from 2026-05-06)

Status legend: 🟢 on track • 🟡 at risk • 🔴 blocked

---

## Objective 1 — Ship PR #72 with green T12 end-to-end

The Dockerfile is correct (rev2). DGD is canonical (rev3). Namespace
alignment works (rev4). One discovery bug remains. Measure success by:
Frontend `/v1/completions` returns 200 with non-empty text on 2-node
Llama-3.1-8B disaggregated serving.

### KR 1.1 — Land Dynamo 1.1.0 image 🟢
- [x] Bump `TRTLLM_IMAGE` + `VLLM_IMAGE` to 1.1.0 in Dockerfile
- [x] CodeBuild triggered (`6fd705f9`)
- [ ] CodeBuild SUCCEEDED → image `dynamo-efa:b2f1c78...` in ECR
- **Timeline:** ~55 min from 02:44 UTC 2026-05-06 = ~03:40 UTC

### KR 1.2 — Validate 1.1.0 on H100 cluster 🟡
- [ ] Run smoke T1–T10 against new SHA (3 min)
- [ ] Run T11 NCCL AllReduce (2 nodes, sanity) (5 min)
- [ ] Run T12 Dynamo disagg `/v1/completions` (10 min + 5 min model load)
  - **GO condition:** 200 with non-empty `choices[0].text` → T12 PASS → flip to rev5 evidence
  - **NO-GO condition:** same `0 instances` symptom → file upstream issue
- **Blocker:** requires H100 lock (currently free) + nvshmem suppressor

### KR 1.3 — Close PR #72 🟡
- [ ] If T12 PASS: post rev5 comment flipping status, request Alex review
- [ ] If T12 FAIL on 1.1.0: file upstream ai-dynamo/dynamo issue with
      full evidence pack, link from PR, ship 1.1.0 as "known limitation"
- **Owner action needed:** Alex review once CI green

---

## Objective 2 — Extend roadmap beyond PR #72

### KR 2.1 — Monitor for Dynamo 1.2.0 public release 🟢
Dev.2 exists as `nvstaging` but we cannot consume it (3 blockers below).
When NVIDIA promotes **1.2.0 stable to public NGC**, we bump immediately.
- [x] Poller script committed: `scripts/poll-dynamo-ngc.sh` (awesome-inferencing `b2f929c`) — probes vllm-runtime + tensorrtllm-runtime + frontend for :1.2.0, exit 2 on new tag detected
- [x] Positive-control verified against live nvcr.io (:1.1.0 manifest reads correctly; :1.2.0 absent as expected)
- [ ] Wire as weekly cron (parked until Objective 1 closes)
- [ ] On release: trigger Dockerfile bump + CodeBuild + smoke validation
- **Expected window:** 2–6 weeks from NGC based on upstream cadence

### KR 2.2 — Blackwell B200 hardware access 🟡
DS-V4 models (Flash/Pro) require B200 (TP=4 or TP=8). Our sm_120
fat-binary work is forward-compatible, but no cluster time available.
- [ ] Secure AWS B200 quota (p6e instance?) — external ask
- [ ] Spin up 1-node smoke once hardware lands
- **Status:** deferred, not blocking other work

### KR 2.3 — Retire TRT-LLM on 1.2.0 path 🟡
DS-V4 dev.2 drops TRT-LLM entirely; it's vLLM + SGLang only. Our
combined image ships both backends. Decision needed:
- [x] Decision doc drafted: `docs/KR-2.3-trtllm-on-1.2.0-decision.md` (`a47f11e`) — three paths analyzed (A keep 1.1.0, B add SGLang alongside, C drop TRT-LLM). Provisional pick: Path C.
- [ ] Keep TRT-LLM via NGC `tensorrtllm-runtime:1.1.0` path
- [ ] Add SGLang support to combined image for 1.2.0 alignment
- [ ] Document deprecation path if/when 1.2.0 stable drops TRT-LLM publicly
- **Dependency:** waits on 1.2.0 stable NGC release
- **Status:** upgraded from 🔴 to 🟡 — decision doc ready to execute on trigger

### KR 2.4 — Mirror trivy + syft to AWS public ECR 🟢
CodeBuild 429 rate-limit on Docker Hub (docker.io/{aquasec/trivy,anchore/syft})
was blocking build 6fd705f9. Mirror to AWS public gallery.
- [x] `public.ecr.aws/v9l4g5s4/trivy:{latest,0.70.0}` published
- [x] `public.ecr.aws/v9l4g5s4/syft:{latest,1.44.0}` published
- [ ] Swap `FROM` lines in 4 Dockerfiles (`Dockerfile.efa`, `Dockerfile.dynamo-{combined,vllm,trtllm}-efa`) — nemo2's call on timing (4b18f907 already SUCCEEDED, so this is pre-emptive for the next build)

---

## Objective 3 — DeepEP / MoE parallel track (dyno session context)

### KR 3.1 — deepep-base:v2 build + validate 🟢
Designed in `base/deepep-base/DESIGN-V2.md` (commit `308f9d5`). Precompiled
kernel cache + 3-variant torch ABI (2.6/2.7/2.10). **All scripting is staged** —
one operator command away from fire-when-ready.
- [x] Dockerfile.v2 lint-clean via `docker buildx --check`
- [x] `base/deepep-base/scripts/generate-v2-sbom.sh` (`be611cd`) — SPDX + CycloneDX + Trivy
- [x] `base/deepep-base/scripts/v2-derived-sweep.sh` (`d79487a`) — rebuild 6 derived images + `test_deepep_fwd.py` per image
- [x] `base/deepep-base/scripts/v2-release.sh` (`dd11302`) — master runbook, `--dry-run` verified
- [x] `base/deepep-base/scripts/v1-v2-sbom-diff.sh` (`bc0fcbd`) — auto-WARNs on CVE regression, gates rc1 → v2 promotion
- [x] `base/deepep-base/scripts/README.md` (`a81a3d5`) — operator-facing runbook docs
- [ ] Build on H100 after PR #72 lands (task #64) — **GATED on O1 close**
- [ ] Generate SBOM + Trivy scan (task #66) — fires inside release chain
- [ ] Sweep 5+ derived images on new base (task #67) — fires inside release chain
- [ ] Build `trtllm-deepep:v1` on v2 (task #68) — sweep picks it up automatically
- **Timeline:** ~85 min cold / ~25 min warm per scripts README
- **Status:** upgraded from 🟡 to 🟢 — all prep done, awaiting gate

### KR 3.2 — Align DeepSeek-V3 (current) MoE baselines 🟢
We already have working DeepEP + NVSHMEM images for H100. Keep them
operational while v2 base lands.
- [x] `examples/deepep-efa/` + `examples/vllm-deepep-working/` proven
- [ ] Re-verify after deepep-base:v2 builds (no regression gate)

### KR 3.3 — Prepare for DeepSeek-V4 (B200 era) 🟢
Our sm_120 fat-binary is the only concrete DS-V4 prep. Everything else
waits on hardware + public tags.
- [x] NCCL compiled with sm_100 + sm_120 gencode (commit 81f61cc)
- [x] Skills documented (nccl-fatbinary-check, h100-smoke-harness)
- [ ] B200 smoke template when hardware lands

---

## Today's focused execution (next 2 hours)

**Sequential, highest-leverage:**

1. **Wait for build `6fd705f9`** to complete (~35 min remaining) — KR 1.1
2. **Run rev5 validation** end-to-end on new SHA (~25 min) — KR 1.2
3. **Post rev5 PR comment** with outcome — KR 1.3
4. **Set up 1.2.0 NGC poller** (one-time cron) — KR 2.1

**Explicit non-goals today:**
- No 1.2.0-dev bump (nvstaging auth + B200 blockers)
- No deepep-base:v2 build (waits for PR #72 merge per task #64 charter)
- No further rev-N experiments on 1.0.1 (sunk cost; 1.1.0 replaces it)
